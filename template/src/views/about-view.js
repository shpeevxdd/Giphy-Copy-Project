/**
 * Generates the HTML markup for the About page.
 *
 * @returns {string} HTML string representing the About view.
 */
export const toAboutView = () => `
  <div class="about-section">
    <h1 class="section-title">About</h1>
    <p>A lightweight GIF browser built on the GIPHY API. Browse trending GIFs,
       search by keyword, save favorites, and upload your own.</p>
  </div>
`;
